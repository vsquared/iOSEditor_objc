# iOSEditor_objc
This application will allow the user to test run iOS apps on a simulator app (not on an actual device).  Installation of XCode is required for the simulator; '.swift' test code examples using UIKit and SwiftUI are provided.
